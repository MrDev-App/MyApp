{
  "name": "guruvani-firestore-seed",
  "version": "1.0.0",
  "description": "Seed script for GuruVani Firestore — uploads all Shloka data",
  "private": true,
  "scripts": {
    "seed": "node index.js",
    "seed:dry": "node index.js --dry-run",
    "seed:category": "node index.js --category",
    "seed:reset": "node index.js --reset"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
