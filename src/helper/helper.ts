/**
 * Helper re-exports for backward compatibility.
 * Prefer importing directly from '@utils'.
 */
export * from '@utils';
export { triggerHaptic, toHindiNumeral, formatPageNumber } from '@utils';
export type { HapticType } from '@utils';
